name: Pull Request

on:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      #      - name: Lint
      #        run: pnpm lint

      - name: Build
        run: pnpm build

      - uses: supabase/setup-cli@v1
        with:
          version: latest

      - run: supabase start --exclude realtime,studio,inbucket,edge-runtime
      - run: supabase db reset --no-seed <<< 'y'

      - name: Create env file
        run: node scripts.js

      - name: Run Playwright
        run: pnpm playwright

      - name: Upload Playwright Videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-videos
          path: |
            .env.local
          if-no-files-found: ignore
          retention-days: 1
